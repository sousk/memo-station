/ TODO, missing some feautures
%li
  .article_header_subject
    - if header.created_at && header.created_at >= 1.days.ago
      %span.new1 NEW
    - elsif header.created_at && header.created_at >= 2.days.ago
      %span.new2 NEW
      
  .article_header_tag
    - unless header.url.blank?
      = link_to(h(truncate(header.url, :length => URL_TRUNCATE_LENGTH)), header.url)
      %br
  = content_tag :div, '', :id => "article_#{header.id}"
